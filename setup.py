from setuptools import setup, find_packages

setup(
    name="mathquiz",                    # Package name
    version="1.0.0",                    # Version number
    packages=find_packages(),           # Finds the mathquiz package
    install_requires=[],                # Add dependencies if needed
    description="A math quiz for the DSSS homework.",
    author="Julius Becker",                 # Replace with your name
)
